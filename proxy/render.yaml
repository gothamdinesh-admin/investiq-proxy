services:
  - type: web
    name: investiq-proxy
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python claude_proxy.py
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false   # you'll paste this in the Render dashboard
